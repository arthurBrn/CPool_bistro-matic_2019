/*
** EPITECH PROJECT, 2019
** find_priori_sign
** File description:
** Return 1 if the char in parameter is either '*' or '/'
*/

int find_priori_sign(char c)
{
    if (c == '*' || c == '/')
        return (1);
    return (0);
}
